# 高可用架构说明（生产级验收）

## 1. 逻辑架构

```
                    ┌──────────────┐
   App/H5/小程序/PC │  客户端/商户端 │
                    └──────┬───────┘
                           │ HTTPS
                    ┌──────▼───────┐
                    │ API Gateway  │ 无状态 ×N（JWT 鉴权、路由、限流、身份头清洗）
                    └──────┬───────┘
        ┌──────────┬───────┼────────┬─────────┬──────────┬──────────┐
        ▼          ▼       ▼        ▼         ▼          ▼          ▼
     user       product marketing order     pay      settlement aftersale
        │          │       │        │         │          │          │
        └──────────┴── 同步：Feign + Nacos LB + Sentinel 熔断 ──────┘
        ┌──────────────────────────────────────────────────────────┐
        │  异步：RocketMQ 5.x（交易主链路全部事件化，最终一致）        │
        └──────────────────────────────────────────────────────────┘
        ▼          ▼       ▼        ▼         ▼          ▼          ▼
  MySQL 独立 schema（7 库，禁跨库 JOIN）        Redis（缓存/锁/幂等/序列）
        Nacos 注册配置  │  Prometheus + Grafana  │  RocketMQ DLQ + 对账补偿
```

## 2. 高可用手段（逐风险点）

| 风险 | 手段 |
|------|------|
| 单点实例宕机 | 全部组件无状态，K8s 2 副本起 + 反亲和 + PDB；Nacos 心跳自动剔除（默认 5s 心跳、15s 标记、30s 摘除） |
| 网关挂 | 网关多副本 + LB；JWT 无状态鉴权，会话不落在网关 |
| 超卖 | Redis 预扣 + DB `available>=qty` 条件更新 + Redisson 锁三重；库存四态（可售/锁定/占用）记账 |
| 重复下单/重复回调 | `@Idempotent`（Redis SETNX）+ 业务单号唯一索引；支付回调渠道流水号唯一约束 |
| 支付成功后部分下游失败 | MQ 扇出（ORDER_PAID），每消费者幂等 + Broker 重试（16 次递增退避）+ 死信队列 + 日终对账补偿 |
| 支付/订单状态不一致 | 支付域主动对账任务：扫描“已支付无订单状态推进”的支付单重发事件；T+1 渠道对账单比对（长款补单、短款关单、金额不符挂账） |
| 超时任务丢失 | 每条关键超时双保险：RocketMQ 延时消息（15min/30min/24h/10天）+ ShedLock 选主的 DB 扫描任务 |
| 定时任务重复执行 | ShedLock Redis 锁（lockAtLeastFor/lockAtMostFor），多实例只跑一个，宕机锁过期自动漂移 |
| 余额/积分并发 | 账户表行锁 `UPDATE ... SET balance=balance-? WHERE id=? AND balance>=?` + 流水表唯一键（biz_no + change_type）防重 |
| 清算算错 | 金额一律 Long 分；分摊最大余数法保证合计不差 1 分；分账公式单测全量覆盖；清算单状态机不可逆 |
| 消息重复消费 | 消费流水表 `t_<dom>_mq_consume(event_id)` 唯一索引 + 业务幂等双保险 |
| MySQL 故障 | Druid 连接探活+故障转移配置；生产主从半同步 + 只读副本；慢 SQL 监控 |
| Redis 故障 | Redisson 可配哨兵/集群；ID 生成器 Redis 不可用时本机降级；关键扣减最终以 DB 条件更新为准 |
| 突发流量 | Sentinel 网关+服务双层限流；秒杀走 Redis 库存令牌；HPA CPU/内存弹性扩容 |
| 数据安全 | 外部身份头在网关被剥离；手机号等敏感字段脱敏；全链路 traceId；接口归属鉴权（商户只能操作本店数据） |

## 3. 分布式事务策略

- **下单（强一致诉求）**：TCC 思想的补偿模式：
  1. try：锁库存（可售→锁定）、券预核销、积分冻结；
  2. confirm（支付成功，事件驱动）：锁定→占用、券核销、冻结积分扣减；
  3. cancel（取消/超时）：锁定→可售、券释放、冻结积分释放。
  每步都以业务单号幂等；任何步骤失败由补偿任务（MQ 重试 + 定时扫描）最终收敛。
- **支付→订单/库存/营销/积分/清算（最终一致）**：支付回调只保证“支付单成功”落库 + 发 MQ，五方独立幂等消费。
- **退款→清算冲正**：REFUND_SUCCESS 事件驱动，先扣商户待结算，不足扣保证金，佣金/补贴按比例回冲，状态机保护。

## 4. 可观测

- 每服务 `/actuator/health`、`/actuator/prometheus`；Prometheus 抓 JVM/HTTP/Hikari/自定义业务指标。
- 关键业务指标：下单 TPS、回调耗时、库存锁定失败率、MQ 消费积压、对账差异数、提现成功率。
- 日志：JSON 单行（生产），含 traceId/userId/orderNo；关键状态变更全量落状态流水表，可追溯。

## 5. 容量与性能（对照 design 10.1）

- 下单 ≥5000 TPS：无状态服务水平扩容；Redis 原子扣库存；订单号 Redis INCR 序列；批量插入明细。
- 商品详情 ≤200ms：多级缓存（Redis + 本地 Caffeine，由商品服务自行选用）。
- 日终清算 ≤2h：按商户分片 + 批量结算，ShedLock 选主后多线程分页处理。

## 6. 容灾与发布

- 滚动发布 maxUnavailable=0/maxSurge=1 + readiness 通过才接流 + PDB minAvailable=1。
- 配置走 Nacos 配置中心，支持灰度与回滚；数据库变更走 V1/V2 SQL 版本化。
