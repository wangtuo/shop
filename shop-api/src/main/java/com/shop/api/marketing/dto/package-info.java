/**
 * 营销域对外 DTO 契约：价格试算命令/结果、营销资源 TCC 式锁定命令。字段自包含，禁止引用其他域包。
 *
 * <p><b>4.2.1 优惠叠加顺序</b>（按序逐层计算，每层以上一层之后金额为基数）：
 * <pre>
 * 商品原价
 *   → 1. 限时折扣 / 秒杀价（商品级，互斥取最低）
 *   → 2. 满减 / 满折 / 第N件优惠（店铺级；满减与满折互斥取最优）
 *   → 3. 品类券（跨店，1 张）
 *   → 4. 店铺券（1 张）
 *   → 5. 平台通用券（1 张）
 *   → 6. 积分抵现（100 积分=1 元，最高抵商品金额 50%）
 * 实付金额（+ 运费，运费可被免邮券抵扣）
 * </pre>
 *
 * <p><b>4.2.2 分摊</b>：各层优惠按行原价占比（salePriceFen×qty）以最大余数法摊到 SKU，
 * 整单层金额与 {@code ItemPriceDetail} 分摊合计不差 1 分；售后按分摊后实付退款。
 *
 * <p><b>4.2.3 互斥规则</b>：
 * <ul>
 *   <li>秒杀价与所有其他优惠互斥（券、满减、积分均不可用）；</li>
 *   <li>同一层级优惠券只能用 1 张（品类/店铺/平台各 1 张）；</li>
 *   <li>满减和满折互斥，取优惠金额最大者；</li>
 *   <li>拼团订单不可用优惠券、不可用积分抵现（design.md 4.4）；</li>
 *   <li>预售订单优惠券仅尾款阶段可用（presaleFinalStage=true），定金阶段不可用（design.md 4.5）。</li>
 * </ul>
 *
 * <p>所有金额字段为 Long，单位分；集合字段默认空列表（{@code new ArrayList<>()}）。
 */
package com.shop.api.marketing.dto;
