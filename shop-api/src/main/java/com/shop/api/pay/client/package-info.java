/**
 * 支付域对外 Feign 客户端契约包。
 *
 * <p>对应服务 shop-pay-service 的内部接口前缀 {@code /inner/pay}，由订单域（发起支付、
 * 查询支付结果）与售后域（发起退款）同步调用（CONTRACTS.md §3）。</p>
 *
 * <p>支付与退款的关键约束：</p>
 * <ul>
 *     <li>design 6.2：支付结果以第三方异步回调为准，回调必须先验签、再幂等校验，
 *     严禁信任前端回跳；支付域只做验签 / 幂等 / 落单，支付成功后只发 ORDER_PAID 事件，
 *     由订单、商品、营销、用户、清算各域自行幂等消费（CONTRACTS.md §5）；</li>
 *     <li>design 6.4：退款必须原路退回（微信/支付宝/银行卡/云闪付/花呗/白条回原渠道，
 *     余额支付退回余额账户），混合支付按各支付方式实付占比分别退回；
 *     多次部分退款累计不超过实付金额，累计差额用 MoneyUtils 最大余数法保证不差 1 分。</li>
 * </ul>
 */
package com.shop.api.pay.client;
